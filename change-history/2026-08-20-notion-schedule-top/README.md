# 变更总览：我的日程置顶（复刻门户「我的日程」到 Hub 顶部）

> 日期：2026-08-20 ｜ 级别：P2

## 为什么做

- 用户要求把门户「我的日程」（每日课程：时间·课程名·地点）复刻到 Notion Hub 最开头，作为第一屏。

## 改成什么

1. 新增「我的日程」内嵌数据库并置顶（Hub 第 1 个块）：12 列（课程/星期/节次/开始时间/结束时间/地点/教师/周次/学期/状态/来源/备注），34 行 = 课程表 34 行 + 官方浑南作息时间换算（本科教务管理系统「作息时间」模块 2026-08-20 抓取）。
2. Hub 全量重建为 13 库：删除全部块 → 按"我的日程 + 原 12 库"顺序重建 → relation 用 data_sources PATCH 补 → 灌行（开源任务→开源项目映射）→ 顶部插入「📅 我的日程 + 教学周 callout」。
3. 默认视图优化：行按目标显示顺序的逆序创建（默认视图=创建倒序），最终默认显示 = 周一→周日、时间升序；列顺序通过 Edge UI 属性面板拖拽为 课程/星期/开始时间/结束时间/节次/地点/教师/周次/学期/状态/来源/备注。
4. 顶部 callout 同步：🧭 分区清单加入"我的日程"；⭐ 今日聚焦标注"我的日程已上线"。

## 关键行为

- 行删除用 DELETE /v1/blocks/{row_id}（/v1/pages 报 invalid_request_url）；关系补丁必须 data_sources PATCH；默认视图排序=创建倒序（详见 docs/lessons/process-notion-api-first-automation.md 规则 15-17）。
- 门户「更多」完整日程站（/portal/pages/schedulesite）是 Nuxt SSR 空壳，数据以教务课表+校历为源，不依赖门户接口。

## 验证

- API：Hub 33 块顺序正确；13 库行数 34/34/11/0/0/10/5/7/25/2/5/4/5；开源任务 relation 5/5。
- DOM（Edge）：列头顺序正确；首行 = 星期一 09:25-10:10 面向对象程序设计 @ 1号A305。

## 回滚

- 快照：C:/Users/hh/AppData/Local/Temp/notion_snapshot2.json（原 12 库全量数据）；Notion 回收站可恢复被删旧库；视图列序如需还原，UI 手动调整或按 journal 记录重排。
