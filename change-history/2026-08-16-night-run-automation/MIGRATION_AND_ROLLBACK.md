# 迁移与回滚

## 1. 回滚方式

- 删除两条自动化：移除 `C:\Users\hh\.codex\automations\<id>\` 目录（或把 TOML 内 `status` 改为 `PAUSED`）。
- 删除骨架：`git revert` 对应提交（`af9a135`、`c46f558`、`839efa2`）即可，脚本不进集群、无部署影响。

## 2. 运行期风险

- 自动化每天 00:00/04:30 触发；非运行日 prompt 首步空跑退出，但每天仍会唤醒一次会话（几乎零成本）。
- 若用户后续想长期运行，改 TOML 里 rrule 或直接在 UI 暂停；若只跑今晚，可在 UI 将两条暂停。
- `.runtime/night-run/` 完全不入库；删除该目录不影响仓库与集群。