# 升级与回滚

## 1. 迁移

- 无 CRD、数据库变化；控制器升级后下一次分配自动清理残留 QPS。

## 2. 回滚

- `git revert` 本提交即可恢复旧行为；无数据迁移负担。

## 3. 风险与注意

- 清零写入与分配写入同轮进行，若中途 Update 失败会返回聚合错误并重试，最终收敛。
- 删除中实例由 `updateInstanceQPS` 跳过，不阻塞删除。
