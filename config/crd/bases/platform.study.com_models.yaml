---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: models.platform.study.com
spec:
  group: platform.study.com
  names:
    kind: Model
    listKind: ModelList
    plural: models
    singular: model
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .spec.gpuUnits
      name: GPUUnits
      type: integer
    - jsonPath: .spec.maxConcurrency
      name: MaxConcurrency
      type: integer
    - jsonPath: .spec.absoluteScore
      name: AbsoluteScore
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Model 是描述模型基本属性和性能的集群级资源。
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec 定义模型的期望状态，由运维侧配置。
            properties:
              absoluteScore:
                description: 单个已预热副本在理想条件下的能力基准分，用于 Orchestrator 调度
                minimum: 1
                type: integer
              coldStartMs:
                description: 冷启动时间，单位 ms
                minimum: 0
                type: integer
              displayName:
                description: 前端展示名
                minLength: 1
                type: string
              gpuUnits:
                description: 实例占的 GPU 量，比如 800 表示 0.8 卡
                minimum: 1
                type: integer
              maxConcurrency:
                description: 最大并发
                minimum: 1
                type: integer
              performance:
                description: 性能参数，未填写时使用内置默认值
                properties:
                  decodePerTokenMs:
                    default: 20
                    description: 每个生成 token 耗时，单位 ms，默认 20
                    minimum: 1
                    type: integer
                  prefillBaseMs:
                    default: 50
                    description: prefill 基础延迟，单位 ms，默认 50
                    minimum: 1
                    type: integer
                  prefillPerTokenUs:
                    default: 500
                    description: 每个 prompt token 额外增加的 prefill 时间，单位微秒，默认 500µs
                    minimum: 1
                    type: integer
                type: object
            required:
            - absoluteScore
            - coldStartMs
            - displayName
            - gpuUnits
            - maxConcurrency
            type: object
          status:
            description: ModelStatus 运行时状态，由控制器维护
            properties:
              absoluteScore:
                description: |-
                  旧版本的能力基准分，仅用于升级兼容；新配置写入 spec.absoluteScore
                  Deprecated: use spec.absoluteScore.
                type: integer
              conditions:
                description: K8s 标准 conditions，记录一些状态
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
