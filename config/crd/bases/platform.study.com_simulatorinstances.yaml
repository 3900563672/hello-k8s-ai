---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: simulatorinstances.platform.study.com
spec:
  group: platform.study.com
  names:
    kind: SimulatorInstance
    listKind: SimulatorInstanceList
    plural: simulatorinstances
    singular: simulatorinstance
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantRef.name
      name: Tenant
      type: string
    - jsonPath: .spec.modelRef.name
      name: Model
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .spec.timeScale
      name: Rate
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SimulatorInstance 集群级，一个租户下一个模型只对应一个实例
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SimulatorInstanceSpec 实例期望状态
            properties:
              modelRef:
                description: 目标模型
                properties:
                  name:
                    description: 被引用对象的名称
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              replicas:
                description: 副本数
                minimum: 0
                type: integer
              tenantRef:
                description: 所属租户
                properties:
                  name:
                    description: 被引用对象的名称
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              timeScale:
                default: 1
                description: Simulator 离散事件引擎的时间倍速，由 SimulationClock Controller 同步
                maximum: 20
                minimum: 1
                type: integer
              traffic:
                description: 流量配置
                properties:
                  qps:
                    description: 每秒请求数
                    minimum: 0
                    type: integer
                required:
                - qps
                type: object
            required:
            - modelRef
            - replicas
            - tenantRef
            - timeScale
            - traffic
            type: object
          status:
            description: SimulatorInstanceStatus 实例运行时状态，控制器维护
            properties:
              availableReplicas:
                description: 当前 Deployment 中可用的副本数，由 SimulatorInstance 控制器维护
                minimum: 0
                type: integer
              conditions:
                description: 标准的 K8s conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              effectiveScore:
                description: Orchestrator 计算的单副本静态能力分数
                type: integer
              observedAt:
                description: 模拟器最近一次成功发布性能快照的时间
                format: date-time
                type: string
              performance:
                description: 当前性能数据
                properties:
                  queue:
                    description: 队列长度或排队时间
                    properties:
                      unit:
                        description: 单位，比如 "ms"、"s"
                        type: string
                      value:
                        description: 数值
                        type: integer
                    type: object
                  ttft:
                    description: 首 token 时间
                    properties:
                      unit:
                        description: 单位，比如 "ms"、"s"
                        type: string
                      value:
                        description: 数值
                        type: integer
                    type: object
                type: object
              phase:
                description: 运行阶段：Running, Pending, Failed 等
                enum:
                - Running
                - Pending
                - Failed
                - Unknown
                type: string
              reporterID:
                description: 当前性能快照的报告者身份，用于诊断 Lease 选主
                type: string
              score:
                description: 模拟器计算的可用副本池实时能力分数，Traffic 控制器根据它分配 QPS
                type: integer
              simulationElapsedMs:
                description: |-
                  模拟器累计推进的模拟时间（毫秒），由 Simulator Leader 持久化；
                  Leader 切换后继续沿用，冷启动进度不随 reporter 进程重启归零
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
