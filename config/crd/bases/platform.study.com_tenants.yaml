---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tenants.platform.study.com
spec:
  group: platform.study.com
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: string
    - jsonPath: .spec.qps
      name: QPS
      type: integer
    - jsonPath: .spec.ttftThresholdMs
      name: TTFT(Up)
      type: integer
    - jsonPath: .spec.ttftScaleDownThresholdMs
      name: TTFT(Down)
      type: integer
    - jsonPath: .spec.queueThreshold
      name: Queue(Up)
      type: integer
    - jsonPath: .spec.queueScaleDownThreshold
      name: Queue(Down)
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Tenant 表示一个集群级租户资源。
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec 租户的配置，优先级、QPS、扩缩容阈值都在这里
            properties:
              displayName:
                description: 前端显示的名字
                minLength: 1
                type: string
              priority:
                description: 优先级，P1 最高，P5 最低
                enum:
                - P1
                - P2
                - P3
                - P4
                - P5
                type: string
              qps:
                description: 模拟的总 QPS
                minimum: 0
                type: integer
              queueScaleDownThreshold:
                description: 队列长度下限；低于该值时允许缩容，必填
                minimum: 1
                type: integer
              queueThreshold:
                description: 队列长度上限；超过时触发扩容，必填
                minimum: 1
                type: integer
              ttftScaleDownThresholdMs:
                description: TTFT 下限，单位为毫秒；低于该值时允许缩容，必填
                minimum: 1
                type: integer
              ttftThresholdMs:
                description: TTFT 上限，单位为毫秒；超过时触发扩容，必填
                minimum: 1
                type: integer
            required:
            - displayName
            - priority
            - qps
            - queueScaleDownThreshold
            - queueThreshold
            - ttftScaleDownThresholdMs
            - ttftThresholdMs
            type: object
            x-kubernetes-validations:
            - message: ttftScaleDownThresholdMs must be less than ttftThresholdMs
              rule: self.ttftScaleDownThresholdMs < self.ttftThresholdMs
            - message: queueScaleDownThreshold must be less than queueThreshold
              rule: self.queueScaleDownThreshold < self.queueThreshold
          status:
            description: TenantStatus 保存租户的标准 Conditions。
            properties:
              conditions:
                description: 标准的 K8s conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
